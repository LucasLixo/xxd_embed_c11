/* Auto-generated by xxd_embed - do not edit. */
#include <stddef.h>
#include <stdint.h>
#include <stdio.h>
#include <stdlib.h>
#include "xxd.h"

__attribute__((constructor))
static void xxd_register_@FILE_KEY_HASH@(void)
{
    FILE* f = fopen("/xxd/@FILE_KEY@", "rb");
    if (!f) return;
    fseek(f, 0, SEEK_END);
    long sz = ftell(f);
    if (sz < 0) { fclose(f); return; }
    rewind(f);
    char* buf = (char*)malloc((size_t)sz);
    if (!buf) { fclose(f); return; }
    if (fread(buf, 1, (size_t)sz, f) != (size_t)sz) { free(buf); fclose(f); return; }
    fclose(f);
    xxd_add("@FILE_KEY@", buf, (size_t)sz, "@FILE_MIME@");
}
